<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.demo.daoMapper.MybatisMapper">
    <resultMap id="map-01" type="uers01">
        <id property="id" column="id"/>
        <result column="user_name" property="userName"/>
        <result column="cell" property="cell"/>
        <result column="password" property="password"/>
    </resultMap>
<!--    查询是否存在该用户信息 登录-->
    <select id="selectUserName" resultMap="map-01">
        SELECT id, user_name,  password ,cell FROM user WHERE user_name = #{userName} AND password = #{password}
    </select>
    <select id="selectUserCell" resultMap="map-01">
        SELECT id,cell,  password,user_name FROM user WHERE cell = #{userName} AND password = #{password}
    </select>
<!--    用户注册-->
    <insert id="insertEnroll" parameterType="uers01">
            INSERT INTO user (user_name, password, cell) VALUES (#{userName}, #{password}, #{cell})
    </insert>
</mapper>